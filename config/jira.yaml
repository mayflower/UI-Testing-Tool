# UI/UX-Testing-Tool: Jira-Integration
# Trage hier deine Jira-Zugangsdaten ein.
# api_token: Erstelle unter https://id.atlassian.com/manage-profile/security/api-tokens

jira:
  base_url: null        # z.B. "https://yourcompany.atlassian.net"
  email: null           # z.B. "user@company.com"
  api_token: null       # Jira API-Token (kein Passwort)
  project_key: null     # z.B. "PROJ"
  issue_type: Bug       # Standard-Tickettyp: Bug, Task, Story, ...
