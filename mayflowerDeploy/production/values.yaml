environment: production

domain: ui-testing-tool.data.mayflower.tech

app:
  image:
    repository: ghcr.io/mayflower/ui-testing-tool
    tag: latest

config:
  FLASK_ENV: "production"

# secrets: wird aus values.sops.yaml befuellt (FLASK_SECRET_KEY)
secrets: {}
